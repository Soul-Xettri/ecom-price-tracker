import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["img.drz.lazcdn.com", "i.ebayimg.com", "rukminim2.flixcart.com"],
  },
};

export default nextConfig;
